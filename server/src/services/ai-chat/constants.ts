/** Feature namespace shared by all ai-chat prompts & guidance lookups. */
export const FEATURE_AI_CHAT = "ai_chat";

/** Alias — all scenario keys are plain strings. */
export type ScenarioKey = string;
